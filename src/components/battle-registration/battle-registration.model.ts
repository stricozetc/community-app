// tslint:disable-next-line:no-empty-interface
export interface BattleRegistrationProps { }

export interface BattleRegistrationState {
    helpMassage: string;
    buttonMassage: string;
    countWaitPlayers: number;
}